using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class catapult : MonoBehaviour
{
    // This code catapults objects that touch it to land exactly on the specified position in the world.
    Vector3 targetPoint = new Vector3(3, 2, 3);
    float force = 50;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
